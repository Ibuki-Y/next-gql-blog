# Fragment
fragment Post on PostModel {
  id
  title
  type
  publishDate
  emoji
}

query PostIndexPage {
  # Aliases
  articles: posts(type: ["article"]) {
    ...Post
  }
  diaries: posts(type: ["diary"]) {
    ...Post
  }
}
